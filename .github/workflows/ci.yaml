name: CI
on: [push]
jobs:
  eslint:
    name: Lint code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        run: actions/checkout@v2

      - name: Install packages
        run: yarn

      - name: Run ESLint
        run: yarn run lint
